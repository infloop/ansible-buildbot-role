{
  "name": "ansible-buildbot-role",
  "version": "1.0.0",
  "description": "Ansible Buildbot role",
  "license": "Private",
  "private": true,
  "scripts": {
    "test": "docker run --rm -it -v \"$(pwd)\":/tmp/$(basename \"${PWD}\"):ro  -v /var/run/docker.sock:/var/run/docker.sock -w /tmp/$(basename \"${PWD}\") quay.io/ansible/molecule:2.20 molecule test",
    "interactive": "docker run --rm -it -v \"$(pwd)\":/tmp/$(basename \"${PWD}\"):ro  -v /var/run/docker.sock:/var/run/docker.sock -w /tmp/$(basename \"${PWD}\") quay.io/ansible/molecule:2.20 /bin/sh"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/infloop/ansible-buildbot-role.git"
  },
  "author": "Kirill Minenko aka infloop",
  "bugs": {
    "url": "https://github.com/infloop/ansible-buildbot-role/issues"
  },
  "homepage": "https://github.com/infloop/ansible-buildbot-role#readme"
}
